const root=document.documentElement,theme=document.querySelector('#theme'),menu=document.querySelector('#menu'),nav=document.querySelector('#nav');
if(localStorage.getItem('saim-theme')==='dark'){root.classList.add('dark');theme.textContent='☀'}
theme.addEventListener('click',()=>{root.classList.toggle('dark');const dark=root.classList.contains('dark');theme.textContent=dark?'☀':'☾';localStorage.setItem('saim-theme',dark?'dark':'light')});
menu.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('show');observer.unobserve(entry.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
document.querySelector('#contactForm').addEventListener('submit',event=>{event.preventDefault();const data=new FormData(event.currentTarget),subject=encodeURIComponent(`Portfolio message from ${data.get('name')}`),body=encodeURIComponent(`Name: ${data.get('name')}\nEmail: ${data.get('email')}\n\nMessage:\n${data.get('message')}`);location.href=`mailto:saimbashirkhan25@gmail.com?subject=${subject}&body=${body}`});
const progress=document.querySelector('#progress'),header=document.querySelector('header');const updateScroll=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max?scrollY/max*100:0}%`;header.classList.toggle('scrolled',scrollY>12)};addEventListener('scroll',updateScroll,{passive:true});updateScroll();
